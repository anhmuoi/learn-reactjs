import React from 'react';
import PropTypes from 'prop-types';

Pages.propTypes = {
    
};

function Pages(props) {
    return (
        <div>
            
        </div>
    );
}

export default Pages;