import React from 'react';
import PropTypes from 'prop-types';

Page.propTypes = {
    
};

function Page(props) {
    return (
        <div>
            
        </div>
    );
}

export default Page;