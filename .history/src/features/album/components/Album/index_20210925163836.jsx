import React from 'react';
import PropTypes from 'prop-types';

Album.propTypes = {
    
};

function Album({al}) {
    return (
        <div>
            
        </div>
    );
}

export default Album;